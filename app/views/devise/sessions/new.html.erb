<section class="vh-100 gradient-custom">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-12 col-md-8 col-lg-6 col-xl-5">
        <div class="card text-black" style="border-radius: 1rem; background-color: #ece6c0">
          <div class="card-body p-5 text-center">
            <div class="mb-md-5 mt-md-4 pb-5">
              <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                  <h2 class="fw-bold mb-2 text-uppercase"><%= t(:sign_in) %></h2>
                    <p class="text-black-50 mb-5"><%= t(:entering) %></p>
                    <div class="form-outline form-white mb-4">
                      <%= f.label t(:email) %><br />
                      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
                    </div>

                    <div class="form-outline form-white mb-4">
                      <%= f.label t(:password) %><br />
                      <%= f.password_field :password, autocomplete: "current-password" %>
                    </div>
                    
                    <button class="btn btn-outline-dark btn-lg btn-warning px-5" type="submit"><%= t(:sign_in)%></button>
               <% end %>
              <%= render "devise/shared/links" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>